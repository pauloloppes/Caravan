<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.application.caravan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TripDetails"></activity>
        <activity android:name=".TripList" />
        <activity android:name=".TripAdd" />
        <activity android:name=".Account" />
        <activity android:name=".Settings" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PIN" />
        <activity android:name=".PassengerEdit" />
        <activity android:name=".PassengerDetails" />
        <activity android:name=".PassengerList" />
        <activity android:name=".PassengerAdd" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>